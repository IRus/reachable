# Ключ OpenRouteService: https://openrouteservice.org/dev/#/signup
# Только для локальной разработки. В продакшене ключ живёт в окружении
# контейнера и подставляется nginx, в бандл он не попадает никогда.
ORS_API_KEY=
